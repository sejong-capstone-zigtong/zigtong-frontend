import React from "react";
import styled from "styled-components";

const Landing = () => {
  return <TotalComponent>랜딩페이지</TotalComponent>;
};

export default Landing;

const TotalComponent = styled.div``;
